{
  "name": "@fyeeme/pi-subagent-core",
  "version": "0.3.2",
  "description": "Shared dispatch core for spawning pi subprocess agents: spawnAgent, mapWithConcurrencyLimit, createSpawnRegistry, abortAgent, getPiInvocation. Not a pi extension (no `pi` manifest) — a shared library used by pi-review and pi-dynamic-workflows.",
  "type": "module",
  "main": "index.ts",
  "license": "MIT",
  "author": "fyeeme",
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "pi",
    "subagent",
    "spawn",
    "dispatch",
    "multi-agent"
  ],
  "files": [
    "*.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "vitest --run",
    "typecheck": "tsc"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.84.1"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.84.1",
    "@types/node": "22.19.19",
    "typescript": "5.9.3"
  }
}
